name: CI for To-do-list

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '18.x'

      - name: Install dependencies
        run: npm ci

      - name: Lint with ESLint
        run: npx eslint .

      - name: Run tests (if any)
        run: echo "No tests defined"
        # Placeholder for actual test command, e.g., npm test